# Claude Command Boundary

This directory contains repository-specific assistant command prompts.

## Maintenance Notes

- Keep command text aligned with the public component spec and contribution
  rules.
- Prefer updating shared docs first when a rule applies beyond assistant usage.
- Do not place generated outputs or runtime state in this directory.
